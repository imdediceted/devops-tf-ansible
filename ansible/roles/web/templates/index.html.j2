<!doctype html>
<html lang="ru">
  <head>
    <meta charset="utf-8">
    <title>{{ inventory_hostname }}</title>
    <style>
      body { font-family: system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif; margin: 40px; }
      h1 { margin: 0 0 8px; }
      .hint { color: #666; }
    </style>
  </head>
  <body>
    <h1>Привет, это задача Александра</h1>
    <div class="hint">Хост: {{ inventory_hostname }}</div>
  </body>
</html>
